[pytest]
asyncio_mode = auto
testpaths = tests

filterwarnings =
    ignore::DeprecationWarning
    ignore::PendingDeprecationWarning

markers =
    integration: marks tests that hit the real NeonDB (deselect with -m "not integration")